const paths = {
  saveDirPath: "data",
};

const query = {
  BLOG: "write a ready to publish blog, in markdown syntax, with proper headings, code snippets(if required) and examples on the topic",
  COVER: "generate a cover image for a blog on the topic",
};

module.exports = { paths, query };
