const paths = {
  saveDirPath: "data",
};

const query =
  "write a ready to publish blog, in markdown syntax, with proper headings, code snippets(if required), examples on";

module.exports = { paths, query };
